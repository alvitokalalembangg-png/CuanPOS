<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="16dp"
    app:cardCornerRadius="16dp"
    app:cardBackgroundColor="#F5EFF5"
    app:strokeWidth="0dp"
    style="@style/Widget.Material3.CardView.Filled">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Riwayat Antrean Bill"
            android:fontFamily="@font/open_sauce_medium"
            android:textColor="#000000"
            android:textSize="18sp"
            android:textStyle="bold"
            android:layout_marginBottom="12dp"/>

        <!-- Container untuk daftar bill (Diisi dinamis via Kotlin) -->
        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="250dp"
            android:layout_marginBottom="16dp">

            <LinearLayout
                android:id="@+id/containerRiwayat"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical" />
        </ScrollView>

        <!-- Tombol Tutup -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnTutupRiwayat"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:text="TUTUP"
            android:fontFamily="@font/open_sauce_medium"
            android:backgroundTint="#0039ff"
            android:textColor="#FFFFFF"
            app:cornerRadius="24dp"/>
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>